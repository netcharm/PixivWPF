<mah:MetroWindow x:Class="PixivWPF.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:local="clr-namespace:PixivWPF"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 mc:Ignorable="d"
                 Icon="pack://application:,,,/PixivWPF;component/Resources/pixiv-icon.ico"
                 BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                 BorderThickness="1"                 
                 Title="Pixiv" 
                 VisualTextRenderingMode="Auto"
                 RenderOptions.BitmapScalingMode="HighQuality"
                 RenderOptions.CachingHint="Cache"
                 RenderOptions.ClearTypeHint="Enabled"
                 RenderOptions.EdgeMode="Aliased"
                 TextOptions.TextFormattingMode="Display"
                 AllowDrop="True"
                 WindowStartupLocation="CenterScreen" MinWidth="1484" MinHeight="720" Width="1484" Height="968"
                 KeyUp="MetroWindow_KeyUp"
                 PreviewDragOver="MainWindow_DragOver" PreviewDrop="MainWindow_Drop"
                 StateChanged="MetroWindow_StateChanged" 
                 StylusUp="MetroWindow_StylusUp"
                 Closing="MainWindow_Closing" >

    <mah:MetroWindow.Resources>
        <Style x:Key="MetroSplitButton" TargetType="{x:Type mah:SplitButton}">
            <Setter Property="Background" Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Accent}" />
                    <Setter Property="BorderThickness" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>

    </mah:MetroWindow.Resources>
    
    <mah:MetroWindow.Flyouts>
        <mah:FlyoutsControl>

        </mah:FlyoutsControl>
    </mah:MetroWindow.Flyouts>

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands ShowLastSeparator="True">
            <Button x:Name="CommandNav" x:Uid="CommandNav" 
                    Click="CommandNav_Click"
                    ToolTip="Open Navigation Flyout">
                <iconPacks:PackIconModern Kind="PageSearch" />
            </Button>
            <Button x:Name="CommandNavRefresh" x:Uid="CommandNavRefresh" 
                    Click="CommandNavRefresh_Click"
                    ToolTip="Refresh Current Page">
                <iconPacks:PackIconModern Kind="Refresh" />
            </Button>
            <Button x:Name="CommandNavRefreshThumb" x:Uid="CommandNavRefreshThumb" 
                    Click="CommandNavRefresh_Click"
                    ToolTip="Refresh Current Page Thumbnail">
                <iconPacks:PackIconModern Kind="ImageMultiple" />
            </Button>
            <Button x:Name="CommandNavDate" x:Uid="CommandNavDate" 
                    Click="CommandNavDate_Click"                                
                    ToolTip="Date Selector">
                <iconPacks:PackIconModern Kind="Calendar" />
            </Button>
            <Button x:Name="CommandNavPrev" x:Uid="CommandNavPrev" 
                    Click="CommandNavPrev_Click"
                    ToolTip="Goto Prev Page" Visibility="Collapsed">
                <iconPacks:PackIconModern Kind="NavigatePrevious" />
            </Button>
            <Button x:Name="CommandNavNext" x:Uid="CommandNavNext" 
                    Click="CommandNavNext_Click"
                    ToolTip="Goto Next Page">
                <iconPacks:PackIconModern Kind="NavigateNext" />
            </Button>
            <TextBlock x:Name="NavPageTitle" x:Uid="NavPageTitle" 
                       HorizontalAlignment="Left" VerticalAlignment="Center"
                       Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"/>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands ShowLastSeparator="False">
            <ComboBox x:Name="SearchBox" x:Uid="SearchBox" 
                      Width="160" Height="16"
                      IsEditable="True" 
                      ItemsSource="{Binding Path=AutoSuggestList}"
                      Style="{DynamicResource MahApps.Styles.ComboBox}"
                      mah:TextBoxHelper.ClearTextButton="True"
                      mah:TextBoxHelper.Watermark="Searching ..." 
                      IsTextSearchEnabled="False"
                      SelectionChanged="SearchBox_SelectionChanged"
                      TextBoxBase.TextChanged="SearchBox_TextChanged"
                      KeyDown="SearchBox_KeyDown" DropDownOpened="SearchBox_DropDownOpened" >
            </ComboBox>
            <Button Click="CommandSearch_Click" 
                    ToolTip="Searching Illust/User">
                <iconPacks:PackIconModern Kind="Magnify" />
            </Button>
            <Button Click="CommandDownloadManager_Click" 
                    ToolTip="Open Download Manager">
                <iconPacks:PackIconModern Kind="Download" />
            </Button>
            <ToggleButton x:Name="CommandToggleDropbox" Click="CommandToggleDropbox_Click">
                <iconPacks:PackIconModern Kind="Link" />
            </ToggleButton>
            <Button Click="CommandLogin_Click"
                    ToolTip="Open Login Dialog">
                <iconPacks:PackIconModern Kind="User" />
                <!--<Rectangle Width="16" Height="16" Fill="{DynamicResource MahApps.Brushes.IdealForeground}">
                    <Rectangle.OpacityMask>
                        <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_user}"/>
                    </Rectangle.OpacityMask>
                </Rectangle>-->
            </Button>
            <Button Click="CommandToggleTheme_Click" >
                <iconPacks:PackIconModern Kind="Contrast" />
            </Button>
            <mah:SplitButton x:Name="CommandToggleTheme"
                             ToolTip="Toggle Application Theme"
                             MinWidth="96"
                             Background="Transparent"
                             AlternationCount="2"
                             BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                             Click="CommandToggleTheme_Click" 
                             SelectionChanged="CommandToggleTheme_SelectionChanged">

                              <!--ArrowBrush="{DynamicResource IdealForegroundDisabledBrush}"
                                  Foreground="{DynamicResource IdealForegroundDisabledBrush}"
                                  Background="Transparent" 
                                  Style="{DynamicResource MetroSplitButton}"
                                  BorderThickness="1" BorderBrush="{Binding Path=WindowTitleBrush, Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Controls:MetroWindow}}}"-->

                <mah:SplitButton.Icon>
                    <iconPacks:PackIconModern Kind="ClothesShirt" />
                </mah:SplitButton.Icon>
            </mah:SplitButton>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!--<Frame Grid.Column="0" x:Name="NavFrame" NavigationUIVisibility="Visible">
            <Frame.CacheMode>
                <BitmapCache EnableClearType="True" SnapsToDevicePixels="True"/>
            </Frame.CacheMode>
        </Frame>-->
        
        <Frame Grid.Column="1" x:Name="ContentFrame">
            <Frame.CacheMode>
                <BitmapCache EnableClearType="True" SnapsToDevicePixels="True"/>
            </Frame.CacheMode>
        </Frame>
        <!--<mah:Flyout Grid.Column="0" Grid.ColumnSpan="2" 
                    x:Name="NavFlyout" x:Uid="NavFlyout" Header="Navigation" 
                    Position="Left" Width="Auto" 
                    IsAutoCloseEnabled="True" AutoCloseInterval="5000"
                    CloseButtonVisibility="Visible" ExternalCloseButton="Left"
                    Theme="Adapt" Opacity="0.95"
                    Foreground="{DynamicResource MahApps.Brushes.Flyout.Foreground}" 
                    Background="{DynamicResource MahApps.Brushes.Flyout.Background}"
                    DataContextChanged="NavFlyout_DataContextChanged">
        </mah:Flyout>-->
    </Grid>
</mah:MetroWindow>
